const config = {
    //endpoint can be either localhost or a deployed worker.
    //Typically, it is reccomended you keep the localhost value.
    "endpoint":"http://127.0.0.1:8787/",

    //filename is relative to the bucket, the file should exist in this location.
    //The folder and filenames are only examples, and can be what you want.
    "filename":"bucket-path-to/example.file",

    //do not edit this line, unless you have a file with this name in your bucket.
    "invalidfile":"invalid-file-12345.jpg"
}

module.exports = config